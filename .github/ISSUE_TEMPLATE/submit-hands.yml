name: Submit hand icons
description: Left, right, and spell hand icons for the hands widget (zip of one or more images)
title: "[Hands] "
labels: ["submission", "category:hands"]
body:
  - type: markdown
    attributes:
      value: |
        Attach **one .zip** containing your hand icons. Each file must be named for its
        role — either `<role>.png` or `<anything>_<role>.png`. Roles:
        `lefthand` (open left hand), `righthand` (open right hand), `spellhand` (hand with
        a rune circle above the palm).
        A partial set is fine. Each icon should be a single bold pictogram readable at
        20px. Solid black backgrounds are made transparent automatically. Your files will
        be published as `<set name>_<role>.png`.
  - type: input
    id: name
    attributes:
      label: Set name
      description: Lowercase letters, numbers, `_` and `-` only. Prefixes every file in the set.
      placeholder: inkline
    validations:
      required: true
  - type: input
    id: author
    attributes:
      label: Author credit
      description: Shown in the gallery.
    validations:
      required: true
  - type: textarea
    id: description
    attributes:
      label: Description
  - type: input
    id: tags
    attributes:
      label: Tags
      description: Comma-separated.
  - type: textarea
    id: image
    attributes:
      label: Image set (zip)
      description: Drag and drop one .zip here.
    validations:
      required: true
  - type: input
    id: threshold
    attributes:
      label: Background threshold (advanced)
      description: >-
        Leave blank normally. If the processed results show a dark halo, edit this to a
        higher value like 40 and the submission re-runs; if dark details get erased,
        lower it. Applies to every image in the zip.
